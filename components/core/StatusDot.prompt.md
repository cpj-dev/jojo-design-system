A small filled status circle — the brand's preferred state indicator (not an icon, not emoji).

```jsx
<StatusDot tone="ok" label="Operational" />
<span><StatusDot tone="warning" /> Degraded</span>
```

Tones: `neutral | ok | info | warning | danger | accent`. Pair with a text label rather than relying on color alone.
