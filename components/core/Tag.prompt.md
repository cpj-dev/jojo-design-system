Full-pill chip for filters / selected tokens — quieter than Badge. Pass `onRemove` for a × control.

```jsx
<Tag>TypeScript</Tag>
<Tag leadingIcon={<StatusDot tone="ok" />}>production</Tag>
<Tag onRemove={() => remove(id)}>has:label</Tag>
```
