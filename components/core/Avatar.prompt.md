Circular avatar — image or initials. Use in nav, comments, member lists.

```jsx
<Avatar src="/u/ada.jpg" alt="Ada Lovelace" />
<Avatar initials="AL" size="sm" />
```

Sizes: `sm` 24 / `md` 32 / `lg` 44. Falls back to `initials` when `src` is absent.
