Pill segmented control for 2–3 short options; active item = subtle pill + ink text. Use Tabs for primary navigation.

```jsx
<SegmentedControl value={view} onChange={setView}
  options={[{value:'list',label:'List'},{value:'board',label:'Board'}]} />
```
